[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "shinobi-generator"
version = "0.1.0"
description = "Pygame shinobi character generator"
requires-python = ">=3.10"
dependencies = ["pygame>=2.6"]

[tool.setuptools.packages.find]
where = ["src"]
